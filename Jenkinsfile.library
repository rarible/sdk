def testSuccess = false

pipeline {
  agent none

  options {
    disableConcurrentBuilds()
  }

  stages {
    stage('test') {
      environment {
	      NPM_TOKEN = "na"
      }
      when { branch "master"  }
      agent any
      steps {
        sh 'yarn && yarn build && yarn test'
      }
    }
    stage('build and deploy') {
      agent any
      when { tag "v*"  }
      environment {
        NPM_TOKEN = credentials('npm-token')
        NPM_HOME = '${env.WORKSPACE_TMP}'
      }
      steps {
          sh 'yarn && yarn build'
          sh 'npm config set //registry.npmjs.org/:_authToken=${NPM_TOKEN} --location project'
          sh 'yarn release'
      }
    }
  }
  post {
    always {
      node("") {
        cleanWs()
      }
    }
  }
}
