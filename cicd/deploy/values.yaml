transaction-backend:
  image:
    repository: sdk
  replicas: 1
  service:
    enabled: true
  livenessProbe:
    enabled: false
  environments:
    SDK_ENV: '{{ include "service.appEnv" $ }}'
  updateConfig:
    order: start-first
  nodeSelector:
    node.labels.rarible.service: true
  networks:
    additional:
    - proxy-staging